# Findings register

Point 151: every finding carries the same eleven fields, whatever found it — a scanner, a
fuzz run, a failing test, a dependency advisory or a person reading the diff. The register is
one table so that "what is open, and how bad is it" is one question with one answer.

`node scripts/security.mjs scan` reads this file and fails the build if a row is incomplete,
if a severity or status is not one of the values below, if a `fixed` row names a regression
test that does not exist (point 158), if a `fixed` row is missing from
`docs/SECURITY_CHANGELOG.md` (point 178), or if a **CRITICAL or HIGH row is still open**
(point 152). That last rule is the release block, and the only way past it is a status of
`accepted` whose *Fix* column says where the decision is written down.

**Severity** (point 152): `CRITICAL`, `HIGH`, `MEDIUM`, `LOW`, `INFO`.
**Status**: `open`, `fixed`, `accepted`, `not-applicable`.

Severity here is about this deployment, not about a CVSS vector: what an attacker gets, how
much they need first, and what it costs the people using the service. A defect that needs a
stolen administrator session to matter is not the same finding as one a stranger can trigger
with a header.

| ID | Severity | Component | Source | Description | Attack path | Impact | Likelihood | Fix | Regression test | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SEC-2026-001 | MEDIUM | `src/server/lib/cookies.ts`, `src/client/api.ts` | header fuzzing, `test/fuzz.test.ts` (block 141–180) | `decodeURIComponent` on a cookie value throws on a malformed escape (`csrf=%zz`, a truncated UTF-8 sequence, a lone `%`), and cookies are parsed before authentication on every request | set or inject one malformed cookie for the origin — a related host can, and so can anything else running on it — then every request from that browser is answered by the error handler | every route answers 500 `internal_error` instead of the 401/403 it was going to; one error line per request in the operator's log, which is how a bad header becomes an incident; the browser is unusable until the cookie is cleared | high: one header, no account, no timing | guarded decode in both places — a value that will not decode is kept verbatim and refused by the check that was going to refuse it | `test/fuzz.test.ts` | fixed |
| SEC-2026-002 | MEDIUM | `src/server/routes/auth.ts` | reading the register path while writing the authorization matrix (block 141–180) | the first account of a deployment becomes `admin` after a `SELECT` that is not in the transaction that inserts, so two registrations racing on an empty database both saw an empty `users` table | register twice in the same instant against a brand-new deployment, before or with the operator | a second, unintended administrator at genesis: seller approvals, treasury reads, canary publishing, role changes | low: needs the deployment's first moment and a race, but the window is real on PostgreSQL under READ COMMITTED | the claim is a row in `bootstrap_claims`, taken with `INSERT … ON CONFLICT DO NOTHING RETURNING` inside the same transaction as the account (migration 027, ADR-0104) | `test/authz_fuzz.test.ts` | fixed |
| SEC-2026-003 | LOW | `src/shared/crypto/ratchet.ts` | parser fuzzing, `test/fuzz.test.ts` (block 141–180) | `decodeMessage` read `.v` off the result of `JSON.parse`, so `null`, `7` and `"x"` produced a `TypeError` from inside the parser instead of its own refusal | post an envelope whose payload is valid JSON but not an object; the recipient's client decodes it | none today — `receiveMessages` catches per envelope and acknowledges — but a parser that throws the wrong error is one refactor away from a caller that does not expect it | high to trigger, no consequence | shape check before the fields, and the parser's own `Error` | `test/fuzz.test.ts` | fixed |
| SEC-2026-004 | INFO | `src/server/routes/moderation.ts` | the staff-route sweep in `test/authz_fuzz.test.ts` (block 141–180) | `POST /api/moderation/reports` sits under the staff prefix and is deliberately open to any authenticated account — filing a report is a user action, reading the queue is not | none: the route is authenticated and rate-limited, and it writes a report owned by the caller | naming only: a reviewer scanning the route table for staff-only paths can misread which routes are privileged | n/a | kept as is, and named in the sweep's allowlist with the reason, so the exception is visible instead of implied. Moving it would break clients for a cosmetic gain (`docs/API.md`) | `test/authz_fuzz.test.ts` | accepted |
| SEC-2026-005 | LOW | `src/server/security.ts` | the `timing-unsafe-secret-compare` rule, `scripts/security.mjs` (block 141–180) | the CSRF double-submit token was compared with `!==`, which returns as soon as two bytes differ; two other places had each grown a private constant-time helper | measure the response time of a cross-site request while guessing the token byte by byte — awkward through a browser, and the token is 24 random bytes | small on its own; the reason to fix it is that a `!==` on a secret is what the next handler copies | low | one `constantTimeEqual()` in `src/server/lib/ids.ts`, used by the CSRF check, the proof-of-work MAC and the payout worker's bearer token; the static rule keeps a fourth copy from appearing | `test/security_pipeline.test.ts` | fixed |
| SEC-2026-006 | INFO | `src/server/routes/deliveries.ts` | the stranger-identifier sweep in `test/authz_fuzz.test.ts` (block 141–180) | `GET`/`DELETE /api/attachments/:id` have no owner column: any authenticated caller who presents an id may read or delete that blob, and an unknown id answers `{deleted: 0}` rather than 404 | guess a 128-bit client-chosen id, inside the `write` rate bucket | with the id: read ciphertext the caller cannot decrypt, or delete bytes the recipient already has. Without it: nothing | negligible — the id is the capability and it is random | unchanged by design (ADR-0043): an owner column would be the recipient list this table exists without. The sweep asserts a stranger's *other* identifiers delete nothing, which is the property that could regress | `test/authz_fuzz.test.ts` | accepted |
| SEC-2026-007 | HIGH | `src/server/app.ts`, `src/server/security.ts` | AppSec audit 2026-09-04, reproduced with `app.inject` | the router percent-decodes static path segments, so `POST /%61pi/auth/logout` ran the handler registered at `/api/auth/logout` while `request.url.startsWith("/api/")` was false: the global `preHandler` returned early and the freeze (ADR-0080), the CSRF check and the session pre-resolution were skipped | percent-encode one letter of `/api/` in any write request; with a session cookie and no CSRF token the request was performed (reproduced: a logout with no token), and with the lockdown engaged every write route was open to the encoded spelling | the operator's breach freeze can be defeated by anyone holding a session, including a stolen one; two of the three CSRF layers gone on every route, leaving `SameSite=Strict` alone | high: one character, no timing, works from curl or a stolen session | `isApiRequest()` decides from `request.routeOptions.url`, the registered pattern, and every hook that gated on the path uses it | `test/security.test.ts` | fixed |
| SEC-2026-008 | HIGH | `src/server/lib/bonds.ts` | AppSec audit 2026-09-04 (money/concurrency) | `releaseBond` and `claimBond` read `sellers.bond_pico`, decided, then wrote; on PostgreSQL under READ COMMITTED two releases racing both read the same stake and both credited it, the ledger's `held_pico` guard being satisfied by any other hold the seller had | an approved seller with a bond of N and any other hold ≥ N (a payout in flight, an order as buyer) fires two `POST /api/market/seller/bond/release` at once | the seller is paid 2N for a stake of N — platform float leaves the building; two claims could likewise overdraw a bond | medium: needs a PostgreSQL deployment and a seller account; trivial to repeat once found | the bond is taken with a compare-and-swap `UPDATE … RETURNING` before the ledger moves (ADR-0106) | `test/bonds.test.ts` | fixed |
| SEC-2026-009 | HIGH | `src/server/routes/auth.ts` | reading `POST /api/auth/delete` against the escrow model | deletion refused only when the deleter's *own* balance was non-empty; on an open order the escrow is held on the buyer's side, so a seller with accepted orders had nothing held, was deleted, and `orders` cascaded away with the account | a seller accepts orders and deletes their account | every buyer's escrow on those orders is held with no order left to settle or release it against — frozen for good, with the order history and evidence gone | medium: one route, one honest mistake or one spiteful seller | deletion also refused (`409 orders_open`) while the account is buyer or seller of an order in `placed`/`accepted`/`delivered`/`disputed` | `test/wallet.test.ts` | fixed |
| SEC-2026-010 | MEDIUM | `src/server/routes/wallet.ts`, migration `028_one_open_payout.sql` | AppSec audit 2026-09-04 (money/concurrency) | "one payout at a time" was a `SELECT` before the transaction, and the automatic ceiling's 24-hour `SUM` cannot see uncommitted rows | several `POST /api/wallet/withdrawals` at once, each under `AUTO_PAYOUT_MAX_XMR` | a balance far above the automatic ceiling leaves in pieces with no administrator's signature — the control that bounds what a stolen session can take | medium: PostgreSQL only, any session | partial unique index `withdrawals_one_open_per_user`, violation mapped to `409 payout_pending` (ADR-0105) | `test/wallet.test.ts` | fixed |
| SEC-2026-011 | MEDIUM | `src/server/lib/rate_limit.ts` | AppSec audit 2026-09-04 (money/concurrency) | `consume` read the bucket, decided in JavaScript, wrote `tokens − cost`; k concurrent requests cost one token on PostgreSQL | send the burst in parallel rather than in series, on any bucket (`login`, `register`, `upload_bytes`, `wallet_write`) | every quantitative control softened by a factor the attacker chooses | medium: PostgreSQL only; no account needed for the anonymous buckets | one `INSERT … ON CONFLICT DO NOTHING` then one conditional `UPDATE … WHERE refilled >= cost RETURNING` (ADR-0106) | `test/limits.test.ts` | fixed |
| SEC-2026-012 | MEDIUM | `src/server/routes/bonds.ts`, `src/server/routes/market.ts` | AppSec audit 2026-09-04 (authorization) | the bond claim did not check that the deciding moderator was not the buyer or seller of the order, and its "reported" precondition accepted a report from any account against any order id; the status route gave a moderator the `moderator` actor role on their own orders | a moderator buys from a bonded seller, completes, reports the order from their own account, and claims up to the price from the bond — repeatable per order; or settles their own dispute | a moderator drains an honest seller's bond by the value of orders they place themselves | low: needs a moderator account, but the audit row records it rather than preventing it | a party to the order is refused the claim (`403`) and acts only as that party on the status route; the qualifying report must be the buyer's own (ADR-0108) | `test/bonds.test.ts` | fixed |
| SEC-2026-013 | MEDIUM | `src/server/lib/bonds.ts` | AppSec audit 2026-09-04 (money) | a bond was held only while an order was `disputed`; a completed order cannot be disputed, so the bond was releasable exactly when a buyer's post-completion report was waiting for a moderator | complete, get reported, `POST /api/market/seller/bond/release` before the moderator gets to the queue | the bond mechanism defeated in the one scenario it exists for; the trust signal in listings is retractable at the moment it would be used | medium: any established seller, no timing | release refused while a buyer's own report on one of the seller's orders is `open`; `openReports` in the bond status (ADR-0108) | `test/bonds.test.ts` | fixed |
| SEC-2026-014 | MEDIUM | `src/server/lib/cookies.ts`, `src/server/app.ts`, `src/server/security.ts`, `src/client/api.ts` | AppSec audit 2026-09-04 (sessions) | the session and CSRF cookies had no `__Host-` prefix, so a cookie set by any host under the registrable domain with a `Domain` attribute is indistinguishable from this origin's own | a related host sets `session=<attacker token>; Domain=…`; the victim's browser sends the attacker's session and everything the victim does lands in the attacker's account | account confusion and data capture without a stolen credential; the CSRF double-submit token becomes attacker-chosen | low: needs a foothold on a sibling host, which SEC-2026-001 already treats as in scope | `__Host-session` / `__Host-csrf` whenever the response is `Secure`; bare names only on onion hosts (ADR-0107) | `test/hardening.test.ts` | fixed |
| SEC-2026-015 | MEDIUM | `src/client/messaging.ts`, `src/client/incoming.ts`, `src/client/views/chat.ts`, `src/client/api.ts`, `src/client/ui.ts` | AppSec audit 2026-09-04 (client) | the decrypted payload was cast, not checked; a message without a string `text` was stored in the vault and every render of the list threw from `last.text.slice()` after `clear(root)` | one account with an edited client sends one authentic envelope whose plaintext has no `text` | the victim's Messages screen is blank on every load and login until localStorage is cleared by hand; search throws the same way | high: any account, one envelope, persistent | `parseIncoming()` and a full `validAttachment()` before anything is stored, after the ratchet state is committed; the 428 `pow` body is validated (`solvablePow`) and `safeUrl` refuses `/\host` | `test/abuse.test.ts` | fixed |
| SEC-2026-016 | MEDIUM | `scripts/backup.mjs` | AppSec audit 2026-09-04 (infrastructure), reproduced | every backup command materialised the plaintext database in the shared temp directory with default permissions (`0644`) for the seconds it took to encrypt or inspect it | another login on the host runs `cp /tmp/symvolon-*.sqlite ~` in a loop | the whole database in the clear: hashes, ledger, orders, sellers — the artefact the backup pipeline promises never exists unencrypted | low on a single-tenant host, high on a shared one | every scratch inside a `mkdtemp` directory (`0700`), `0600` itself, removed recursively in `finally` | `test/backup.test.ts` | fixed |
| SEC-2026-017 | LOW | `src/server/lib/sessions.ts` | AppSec audit 2026-09-04 (sessions), reproduced | the daily rotation was an unconditional `UPDATE`; parallel requests on the first request of a day each minted a token, the last write won, the browser usually kept a dead one | load the app after midnight UTC | unexplained sign-out sixty seconds later, read by users as a breach; each losing rotation re-armed the previous token's grace window | high, no attacker needed | compare-and-swap on the presented hash, `RETURNING id`; only the winner hands back a cookie (ADR-0106) | `test/sessions.test.ts` | fixed |
| SEC-2026-018 | LOW | `src/server/config.ts` | AppSec audit 2026-09-04 (configuration) | fifteen numeric limits were read with a bare `Number()`; `SESSION_TTL_MS=30d` booted with `NaN` and either broke every sign-in with a misleading 409 or, where the value reached a comparison, disabled the ceiling silently | an operator typo | a sign-in outage diagnosed as a database problem, or idle/absolute expiry off | low: needs a configuration mistake | `positiveInteger()` / `nonNegativeInteger()` for every one of them; boot fails naming the variable | `test/environments.test.ts` | fixed |
| SEC-2026-019 | LOW | `src/server/routes/*.ts`, `src/server/lib/rate_limit.ts` | AppSec audit 2026-09-04 (authorization sweep) | sixteen routes charged no bucket — among them the vault download, envelope acknowledgement, device revocation and the three payout-worker routes, the last an anonymous request amplifier with a DB read before the bearer check | loop the un-metered route | per-account resource exhaustion invisible to the limiter; the worker routes cost the server for free | medium | every route charges the bucket `docs/API.md` named; a `payout_worker` bucket keyed on address; `POST /api/keys/revoke` records `device.revoked`; a sweep over the route files with a written allowlist | `test/limits.test.ts` | fixed |
| SEC-2026-020 | LOW | `src/server/lib/ledger.ts` | AppSec audit 2026-09-04 (money) | `claimWithdrawal` moved a queued row with a `NULL` address through the one-way `sending` door and then dropped it | any queued row without a destination (a partially applied state, a future manual path) | the owner's hold frozen until an administrator resolves it by hand, one row per poll | low | the claim selects and guards on `address IS NOT NULL` | `test/monero.test.ts` | fixed |
| SEC-2026-021 | LOW | `scripts/audit.mjs` | AppSec audit 2026-09-04 (supply chain) | `package-lock.json` was skipped by path in both secret scans, and the supply check compared only the URL prefix, so a registry configured with `user:TOKEN@` in its URL would have committed the token in every resolved URL with a green build | `npm install --registry https://user:TOKEN@…`, commit | a real class of leaked credential invisible to the project's own gate | low | two rules (`credential in URL`, `npm auth token`), the lockfile scanned with the key-material rules, the supply check refuses userinfo | `test/audit.test.ts` | fixed |
| SEC-2026-022 | LOW | `.github/workflows/ci.yml` vs `deploy/github-ci.yml` | AppSec audit 2026-09-04 (CI) | the workflow that actually runs is a stale copy: the `postgres` job in `deploy/github-ci.yml` is absent from the live file, so the PostgreSQL suite has not run in CI | a change that breaks only the PostgreSQL path (placeholder rewrite, pooled transactions, a Postgres-only migration) merges green | the driver this audit found three concurrency defects in is the one CI does not exercise; `docs/SELF_CRITIQUE.md` says it runs on every push | n/a — assurance gap | fixed 2026-09-04: the owner copied `deploy/github-ci.yml` over `.github/workflows/ci.yml` (`ebb0e6c`), and the first PostgreSQL run it produced failed on every route — SEC-2026-026, which is the assurance gap this row described, now closed as a finding of its own. `npm run audit` no longer prints the parity notice | `test/security_pipeline.test.ts` (the source of truth), the parity notice in `scripts/audit.mjs` | fixed |
| SEC-2026-023 | LOW | `src/server/routes/messages.ts`, `src/server/lib/send_tokens.ts` | AppSec audit 2026-09-04 (authorization) | sealed-sender tokens have no owner column by design (ADR-0084), so a suspension or deletion cannot revoke them; a stockpile (32 per minute per account) posts envelopes for up to `SEND_TOKEN_TTL_MS` (7 days) afterwards, on a path with no per-request bucket | mint tokens, get suspended, keep sending with `x-send-token` | moderation does not stop delivery for the token TTL; the sealed path is metered at issuance only | low: the recipient can block, the volume is bounded by issuance | accepted by design — ADR-0084/ADR-0089 state the price. Keying the sealed path on address would throttle every onion user together. Operators who want a shorter tail lower `SEND_TOKEN_TTL_MS`; and a global revocation epoch shipped as MD-5 (ADR-0111) — `incident.mjs send-tokens:revoke` invalidates every outstanding token at once, so the tail is now an operator action rather than a 7-day wait. It stays global-only: an ownerless token cannot be revoked per account, so this nukes every stockpile together and clients refetch | `test/sealed_sender.test.ts` | accepted |
| SEC-2026-024 | INFO | `src/client/messaging.ts`, `src/client/incoming.ts` | AppSec audit 2026-09-04 (client) | the sender chooses the channel id; a third account that learns an order's channel id can post an invite into that conversation with a chosen display name | learn the channel id of an order chat (it is the order id, shared with the counterparty) and post an envelope with an X3DH invite | a spoofed line inside an existing conversation, flagged by the AUTH-6 key-change banner | low | fixed (MD-6, ADR-0112): a key the conversation has never seen may open a session in it only if the peer's directory lists it (`GET /api/keys/identity/:username`, which spends no prekey); otherwise the envelope is acknowledged and dropped. A conversation with no peer yet is still trust on first use, as it always was | `test/client.test.ts` | fixed |
| SEC-2026-025 | INFO | `.env.example` | AppSec audit 2026-09-04 (configuration) | `TRUST_PROXY=true` is the shipped example, which believes any `X-Forwarded-For` | expose the app port directly and spoof the header, on a hand-rolled deployment | address-keyed anonymous buckets bypassed | n/a in the shipped topology | not applicable: in `deploy/docker-compose.yml` the app has no published port and Caddy *sets* (not appends) `X-Forwarded-For` from the real peer; naming the proxy by CIDR would break that topology, since the proxy's container address is not fixed. `docs/ENVIRONMENT.md` says to name the proxy on any other topology | `test/environments.test.ts` | not-applicable |
| SEC-2026-026 | HIGH | `src/server/lib/rate_limit.ts`, `src/server/lib/reputation.ts`, six migrations after 012 | first PostgreSQL CI run (2026-09-04, after SEC-2026-022 was closed) | three defects only the PostgreSQL driver has: the limiter's fractional refill rate was bound next to a BIGINT column and typed as one (`invalid input syntax for type bigint`), so **every rate-limited route answered 500** — registration included; six millisecond columns added after migration 012 were still 32-bit INTEGER, so sending a message, committing evidence, minting a sealed-sender token, claiming a payout, a dual approval and the lockdown were 500; `MIN(a, b)` in the suspension penalty is SQLite-only | none needed — the deployment does not work on this driver | availability on PostgreSQL: the server boots and then refuses essentially everything | certain, on every PostgreSQL deployment | `CAST(? AS DOUBLE PRECISION)` on the refill rate; migration `030_widen_timestamps_again.postgres.sql` (also `rate_limits.tokens` to DOUBLE PRECISION); `CASE` instead of `MIN(a, b)`; a static test that every `*_at INTEGER` column has a BIGINT widening, and a lint rule against two-argument `MIN`/`MAX`, so the class fails on SQLite | `test/migrations.test.ts` (the static widening check), `test/limits.test.ts` on the PostgreSQL driver, `scalar-min-max` in `scripts/lint.mjs` | fixed |

## What a row is for

The columns nobody wants to fill in are the ones that make the register useful a year later.
*Attack path* is what an attacker actually does, in order — if it cannot be written, the
finding is a guess and says so. *Likelihood* is about this system: a race that needs the
first second of a deployment is not the same as a header anybody can send. *Regression test*
is the permanent part (point 158): the fix is finished when the test that fails without it
exists, and the register names it so that deleting the test breaks the scan.

## What is not in here

Not every fix is a finding. A hardening change nobody had exploited — a new rule, a tighter
default, a limit — belongs in `docs/DECISIONS.md` and the release notes. This register is for
defects: something that was wrong, with a path from an input to a consequence.

Design decisions that leave a residual risk are not findings either; they are rows in
`docs/THREAT_MODEL.md` and items in `docs/ROADMAP.md`. The two `accepted` rows above are the
boundary case — a sweep flagged them, they were judged, and the judgement is written where
the sweep will show it again.
